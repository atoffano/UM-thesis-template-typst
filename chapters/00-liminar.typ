// chapters/00-liminaire.typ

// Abstracts
// --- Résumé Français ---
#set text(lang: "fr") 
#heading(level: 1, numbering: none)[Résumé]
#v(1em)

// 4000 caractères max (Recommendations de la DRED)
Résumé de la thèse en français. 

*Mots-clés :* Thèse, Typst, Modèle, Université de Montpellier.

#v(2cm)

// --- Abstract Anglais ---
#set text(lang: "en") // Bascule vers l'anglais (formattage)
#heading(level: 1, numbering: none)[Abstract]
#v(1em)

// About 1700 words max (DRED guidelines)
The abstract in English, which should mirror the French content.

*Keywords:* Thesis, Typst, Template, University of Montpellier.

// Retour au français
#set text(lang: "fr")

#pagebreak()

// Remerciements
#heading(level: 1, numbering: none)[Remerciements]
Je tiens tout d'abord à remercier ..